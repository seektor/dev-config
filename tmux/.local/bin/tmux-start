#!/usr/bin/env bash

# Session 1: main — opens in the current directory
tmux new-session -d -s main -c "$(pwd)"

# Session 2: placeholder
tmux new-session -d -s dev-config -c ~/Programming/__DevOps__/dev-config

# Session 3: placeholder
# tmux new-session -d -s <name> -c "<path>"

tmux attach -t main
